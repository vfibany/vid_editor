{
	"name": "remotion-local-editor",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev:server": "nodemon server/index.js",
		"dev:client": "pnpm --prefix client dev",
		"dev": "concurrently --kill-others-on-fail \"pnpm dev:server\" \"pnpm dev:client\""
	},
	"devDependencies": {
		"concurrently": "^10.0.3",
		"nodemon": "^3.1.14"
	},
	"dependencies": {
		"cors": "^2.8.6",
		"express": "^5.2.1",
		"multer": "^2.2.0"
	}
}